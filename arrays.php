<?
    #ARRAY - variable that holds multiple value

    /*
        - Indexed
        - Associative
        - Multi-dimensional
    */

    // Indexed

    $people = array('gauresh', 'pritesh', 'amar');
    echo $people[9];
    $ids = array(12, 15, 18);
    echo $ids[2];
    echo count($ids);
    print_r($ids);

?>